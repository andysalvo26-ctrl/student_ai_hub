% PDF Header for Student AI Hub section documents
% Research-grade typography settings

\usepackage[margin=1in]{geometry}
\usepackage{fontspec}
\usepackage{xcolor}

% Try to use Palatino (common on macOS), fallback gracefully
\IfFontExistsTF{Palatino}{
    \setmainfont{Palatino}
}{
    \IfFontExistsTF{Times New Roman}{
        \setmainfont{Times New Roman}
    }{
        % Use default LaTeX font
    }
}

% Spacing
\setlength{\parindent}{0pt}
\setlength{\parskip}{0.6em}
\linespread{1.25}

% Footer
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{\small\textcolor{gray}{Student AI Hub}}
\fancyfoot[R]{\small\textcolor{gray}{\thepage}}

% Page break control
\widowpenalty=10000
\clubpenalty=10000
